#version 450

layout (local_size_x = 512, local_size_y = 1, local_size_x = 1) in;
layout (bindings = 0) buffer inputBuffer;
{
    uint valuesIn[];
};
layout (bindings = 1) buffer outputBuffer
{
    float valuesOut[];
};


void main()
{
    valuesOut[gl_LocalInvocationID.x] = valuesIn[gl_LocalInvocationID.x] * 1000.0;
}