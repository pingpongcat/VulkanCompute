project('VulkanCompute', 'c')
vlk_dep = dependency('Vulkan')
src = ['main.c',
    'compute.c',
    'instance.c',
    'device.c',
    'pipeline.c']

executable('VulkanCompute', src, dependencies : vlk_dep)
